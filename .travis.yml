language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-digisoft' ]; then cd .. && eval "mv $currentfolder generator-digisoft" && cd generator-digisoft; fi

